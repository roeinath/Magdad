from os import mkdir, path
'''
from matplotlib.pyplot import scatter, xlabel, ylabel, show, savefig, clf

from web_features.tech_miun_temp.Evaluations.MaarihimEvaluations.helper_functions import delete_not_relevants, \
    open_file_and_save_graph


def create_generelized_graph(x_parameters, y_parameters, x_name, y_name):
    name = f'{y_name} as a function of {x_name}'
    x_parameters, y_parameters = delete_not_relevants(x_parameters, y_parameters)
    scatter(x_parameters, y_parameters)
    xlabel(f'{x_name}')
    ylabel(f'{y_name}')


    open_file_and_save_graph(name, 'results/evaluators', name)
    clf()
'''


